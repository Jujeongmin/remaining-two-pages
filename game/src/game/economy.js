/*
 * 엽전과 값.
 *
 * 이 게임에서 돈이 하는 일은 하나다 — 막혔을 때 빠져나갈 길을 사는 것. 그래서 파는 것도
 * 되돌리기와 힌트 둘뿐이다.
 *
 * 버는 길과 쓰는 길이 서로 맞물려 있다. 갑은 되돌리기도 힌트도 안 쓴 판에만 나오므로,
 * 많이 벌려면 사 놓은 것을 안 써야 한다. 사서 편하게 갈 것인가, 아껴서 다음 판을 위해
 * 벌 것인가 — 판마다 그 선택이 선다.
 */

/** 판을 처음 맺을 때 등급별로 받는 엽전. 등급이 오르면 그 차액을 마저 받는다. */
export const COIN_BY_GRADE = { 3: 5, 2: 3, 1: 1 };

/*
 * 프리미엄이 받는 엽전 배수.
 *
 * 순위표에 손대지 않는 자리를 골랐다. 기회를 더 주면 등수를 돈으로 사는 것이 되어
 * 표가 죽는다. 엽전은 제 지갑에서만 도는 것이라, 많이 받아도 남의 등수를 밀어내지 않는다.
 */
export const PREMIUM_COIN = 1.5;

/** 끝없이 풀기는 한 판 넘길 때마다. 본편을 다 깬 뒤에도 벌 길이 남아야 한다. */
export const COIN_PER_ENDLESS = 2;

/** 파는 것. 되돌리기는 자주 쓰는 것이라 싸게, 힌트는 답을 짚어 주는 것이라 비싸게. */
export const PRICE = { undo: 4, hint: 15 };

/** 한 번에 사는 묶음. 낱개로 사려고 여러 번 누르게 하지 않는다. */
export const PACK = { undo: 5, hint: 1 };

/** 광고 한 번에 받는 수. */
export const UNDO_PER_AD = 3;

/** 처음 시작할 때 쥐고 있는 것. */
export const START = { hints: 3, undos: 10, coins: 0 };
