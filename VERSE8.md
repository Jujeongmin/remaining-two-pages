# Verse8 연동 — 남은 두 장

**코드 쪽은 다 붙어 있다.** 남은 것은 콘솔에서 이름 두 개를 만들고 서버를 한 번 올리는
것뿐이다. 아무것도 안 붙어 있어도 게임은 그대로 돌아가고, 광고·순위·상점·계정 저장만
조용히 빠진다.

---

## 이름은 받는 것이 아니라 정하는 것

먼저 이것부터 분명히 해 둔다. 광고 자리와 상품 아이디는 **어디선가 발급받는 값이 아니다.**
우리가 지은 문자열이고, 콘솔에서 하는 일은 **같은 이름에** 값·그림·한도를 붙이는 것이다.

저절로 생기는 것은 verse 하나뿐이다 — `npx @agent8/deploy` 를 돌리면 `.env` 에
`VITE_AGENT8_VERSE` 가 적힌다.

이 게임이 쓰는 이름 (`game/src/platform/config.js` 에 이미 박혀 있다):

| 무엇 | 이름 |
|---|---|
| 광고 자리 | `two-sheets-rewarded` |
| 상품 | `two-sheets-premium` |

---

## 1. 붙는 방식

`game/src/platform/boot.js` 가 셋을 잡아 넘긴다. 화면을 먼저 띄우고 뒤에서 붙는다 —
기다렸다 띄우면 붙을 것이 없는 사람에게는 영영 안 뜨는 화면이 된다.

| 무엇 | 어떻게 |
|---|---|
| 광고 · 상점 | `game/index.html` 의 스크립트 두 줄. `window.Verse8Ads` / `window.VXShop` 을 0.2초마다 10초까지 기다린다 |
| 게임 서버 | `@agent8/gameserver` 를 떼어 받아 `GameServer.getInstance().connect()` |

한 번만 보고 없다고 판단하면 스크립트가 조금 늦게 온 날 광고가 통째로 빠진다. 그래서
기다린다.

> `.env` 는 저장소 루트에 있고 Vite 루트는 `game/` 이다. `vite.config.js` 의 `envDir: '..'`
> 이 없으면 verse 를 못 찾고 `default` 로 붙으려다 실패한다.

---

## 2. 광고 — 보상형만

**전면 광고는 두지 않는다.** 이 게임은 한 판을 들여다보며 궁리하는 놀이라, 판을 맺자마자
광고가 덮으면 방금 세운 것을 볼 틈이 없어진다.

**자리는 하나뿐이다** (`two-sheets-rewarded`). 쓰임은 셋이지만 자리를 쪼개면 콘솔에 셋 다
만들어야 하고 하나라도 빠지면 그 자리만 조용히 안 뜬다. 무엇을 줄지는 자리가 아니라
청구할 때 보내는 이름으로 가른다.

| 언제 | 보내는 이름 | 받는 것 |
|---|---|---|
| 힌트 0인데 힌트 단추 | `hint` | 힌트 1 |
| 되돌리기 0인데 되돌리기 단추 | `undo` | 되돌리기 3 |
| 끝없이 풀기 물림 셋 | `revive` | 기회 1, **한 판에 한 번만** |

`revive` 가 값나가는 자리다. 쌓아 온 기록이 눈앞에서 사라지는 참이라 스스로 누른다.
한 판에 한 번으로 막아 두었다 — 그러지 않으면 광고만 보면 안 끝나는 판이 되어 순위표가
뜻을 잃는다. 이것만 쿨다운을 안 건다. 여기서 막히면 기록이 그대로 끝나기 때문이다.

### 검증 서버를 안 쓴다

`ads-verifier` 는 광고를 막 본 직후에 `pending` 을 준다. `server.js` 는 `setTimeout` 이
없어 잠시 뒤 다시 물어볼 수가 없고, 그러면 **광고를 끝까지 본 사람이 보상을 못 받는다.**
같은 자리에서 이미 깨진 것을 확인했다.

대신 클라이언트가 `rewarded` 를 받은 뒤에만 청구하고 서버가 바로 준다. **서버가 여전히
쥐고 있는 것**: 얼마를 줄지, 얼마나 자주 받을 수 있는지(30초). "봤는지" 만 안 보고
"무한" 은 막는다.

`showRewarded` 는 **기다리는 시간을 걸지 않고** 부른다. `@verse8/ads` 0.5.0 부터 호스트가
답할 때까지 그냥 기다린다. 이쪽에서 시간을 걸면 회선이 느리거나 광고가 길 때 먼저 손을
떼게 되고, 그러면 끝까지 본 사람이 보상을 못 받는다.

판은 `package.json` 과 `index.html` 두 곳에 적혀 있다. 실제로 뜨는 것은 index.html 의
unpkg 스크립트이므로, 올릴 때는 두 곳을 같이 고쳐야 한다. `@latest` 로 두면 어느 날
말없이 판이 바뀐다.

`unsupported_env` 를 받으면 그 세션 동안 광고 단추를 감춘다. 눌러도 아무 일이 없는
단추는 고장으로 읽힌다.

---

## 3. 서버 — `@agent8/gameserver`

```bash
npx -y @agent8/deploy
```

**안 올리면 순위표와 계정 저장이 계속 안 붙는다.** 콘솔에 `connect false` 가 찍히는 것이
그것이다.

### 순위표

- 위에서부터 **스무 명**, **사람마다 한 줄**. 더 높을 때만 갈아 끼우고 예전 줄은 지운다.
- 스무 등 밖이어도 제 등수는 나온다.
- 올라가는 값은 **끝없이 풀기에서 넘긴 판 수**뿐이다.

> 항목을 가리키는 이름은 `id` 가 아니라 **`__id`** 다. 잘못 쓰면 아무 말 없이 아무것도
> 안 지워져서, "사람마다 한 줄" 이 조용히 무너진다.

### 계정 저장

| 값 | 합치는 규칙 |
|---|---|
| 판별 등급 · 열린 판 · 끝없이 최고 | **큰 쪽** |
| 이름 · 복원공 · 말 · 소리 · 음악 | 나중에 손댄 것 |
| 힌트 · 되돌리기 · 엽전 · 프리미엄 | **서버 것만**. 클라이언트가 보내도 무시 |

브라우저 저장을 버리지 않는다. 서버 없이 논 사람의 기록이 접속하는 순간 사라지면 안 된다.
붙는 순간 양쪽을 합치고, 그 뒤로는 바뀔 때마다 조용히 올린다.

**엽전과 갑 덤 힌트도 서버가 얹는다**(`saveProgress`). 판마다 한 번뿐이라 다시 올려도 두 번
얹히지 않는다.

기록 지우기는 서버 것부터 지운다(`wipeProgress`). 여기만 지우고 올리면 큰 쪽을 남기는
규칙 때문에 도로 내려온다. **산 것은 지우지 않는다.**

`server.js` 에 있는 것: `submitScore` · `getTopRankings` · `getMyBestRank` ·
`loadProgress` · `saveProgress` · `wipeProgress` · `getHints` · `spendHint` ·
`spendUndo` · `buy` · `claimEndlessCoins` · `$onItemPurchased` · `claimAdReward`.

---

## 4. 상점 — VXShop

파는 것은 **하나뿐이다**. 힌트는 팔지 않는다 — 돈으로 살 수 있으면 판을 푸는 것이 아니라
사는 것이 되고, 검수 등급도 그만큼 값이 떨어진다.

| 상품 | `two-sheets-premium` |
|---|---|
| 성격 | 한 번 사면 끝 |
| 콘솔 | **Lifetime Limit `1`** |
| 주는 것 | 광고 없이 즉시 보상 · 되돌리기 무한 · 엽전 1.5배 |

**광고 단추를 없애는 물건이 아니다.** 단추는 그대로 두고, 누르면 광고 없이 그 자리에서
보상만 들어온다. 감춰 버리면 그동안 받아 가던 보상까지 같이 사라져 돈을 내고 손해를 보는
꼴이 된다. 산 사람에게는 "광고 없이" 가 값이지 "광고 안 보임" 이 값이 아니다.

판정은 **서버가 넣어 준 값으로만** 한다. `VXShop.getItem()` 의 `purchasedCount` 는 상점
단추를 어떻게 그릴지 정하는 데만 쓴다.

결제 창이 닫히면 `shop.refresh()` → `account.sync()` 로 서버 값을 다시 읽는다. 창이
닫혔다는 것은 샀다는 뜻이 아니다. 지급은 반드시 서버의 `$onItemPurchased` 에서만 한다.

---

## 서버 없이 화면 보기

```
npm run preview
http://localhost:4173/?mock=1
```

모의 서버가 붙는다. 순위표에 남들 기록이 들어 있고, 광고는 검은 막 1.2초, 결제는 0.9초로
흉내 낸다. 왼쪽 아래에 `모의 서버` 표시가 뜬다.

깃발이 없으면 그 꾸러미는 **아예 안 실린다.** 올린 게임에 딸려 가지 않는다.

---

## 지금 남은 것

1. VX Shop 콘솔에 **`two-sheets-premium`** 만들기 — 값·그림·설명, Lifetime Limit `1`
2. Ads 콘솔에 **`two-sheets-rewarded`** 만들기 (콘솔에서 미리 만들어야 하는 경우)
3. `npx -y @agent8/deploy`
