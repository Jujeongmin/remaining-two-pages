# 남은 두 장

설계도가 불타 없어졌다. 남은 것은 **정면도와 측면도 두 장**뿐이고, 그 둘만 보고 건물을
다시 세운다. 도편수가 시키고 견습 복원공이 세운다.

Vite 바닐라. `game/` 이 루트고 `index.html` 이 그 안에 있다. 산출물은 저장소 루트의 `dist/`.

| | |
|---|---|
| 붙이는 법 | [VERSE8.md](VERSE8.md) |
| 그림 주문서 | [ART.md](ART.md) |
| 소리 출처 | [game/public/audio/CREDITS.md](game/public/audio/CREDITS.md) |

---

## 지금 남은 일

### 1. 콘솔에서 만들 것 — **본인**

이름은 이미 코드에 박아 두었다. 콘솔에서는 **같은 이름으로** 만들어 값과 그림만 붙이면 된다.

| 무엇 | 이름 | 어디에 |
|---|---|---|
| 상품 | `two-sheets-premium` | VX Shop. **Lifetime Limit 1** |
| 광고 자리 | `two-sheets-rewarded` | Ads. 자리는 하나뿐이다 |

> 광고 자리를 콘솔에서 따로 만들어야 하는지는 문서에 안 적혀 있다. 안 만들어도 되면
> 그냥 돌고, 만들어야 하면 저 이름으로 하나만 만들면 된다.

### 2. 서버 올리기 — **본인**

```bash
npx -y @agent8/deploy
```

안 올리면 순위표와 계정 저장이 계속 안 붙는다. 지금 콘솔에 `connect false` 가 찍히는 것이
그것이다. `.env` 의 `VITE_AGENT8_VERSE` 는 이미 있다.

### 3. 그림 — 없어도 돌아간다

- **단추와 창 껍데기 10장** ([ART.md](ART.md) 12장). `btn` · `btn-on` · `panel` 셋만 와도
  화면 대부분이 바뀐다
- **견습 표정 10장** — 배경 제거를 다시 해서 주기로 한 것
- 스토어 썸네일

---

## 무엇이 어디에 있나

```
game/src/
  core/     grid.js  levels.js  generator.js      판 · 도면 · 판 뽑기
  render/   camera.js  scene3d.js  plans.js       부지 · 도면 그리기
  input/    pick.js  rotate.js                    누르기 · 돌리기
  game/     stages.js  hero.js  dialogue.js       의뢰 · 복원공 · 대사
            progress.js  economy.js  hint.js      저장 · 엽전 · 힌트
  ui/       screens.js  talk.js  sound.js         화면 · 대화 · 효과음
            bgm.js  ambient.js  style.css         배경 음악 · 판 소리
  platform/ boot.js  backend.js  account.js       Verse8 붙이기
            ads.js  shop.js  leaderboard.js
            config.js  mock.js                    이름 · 모의 서버
  i18n/     ko  en  ja  zh                        222개 열쇠 네 벌
server.js                                         저장소 루트여야 한다
```

---

## 규칙이 서는 자리

읽기 전에 알아 두면 코드가 훨씬 빨리 읽히는 것들.

**도면 두 장으로는 답이 하나로 안 정해진다.** 한 층에서 정면도가 깊이 k칸, 측면도가
가로 k칸을 켜라 하면 그 층의 답은 k! 가지다. 스무 판 중 답이 하나뿐인 판은 없다.
그래서 검수는 원래 배치와 같은지 보지 않고 **두 도면과 자재 수만** 본다. 이야기 쪽도
"두 장에 맞으면 그 집이다" 로 맞춰 두었다.

**자재를 늘리면 오히려 헐거워진다.** k 가 커질수록 그 줄의 답이 k! 로 불어난다. 난이도는
자재가 아니라 **줄 수(H)와 바닥 넓이(N)** 로 올린다. 성문부터는 도면 한쪽 칸 수를 줄여
뽑아, 찍힌 칸만 세어서는 자재 수가 안 나오게 했다.

**층 안에서는 칸이 안 겹친다.** 가림은 위층이 아래층을 덮을 때만 생긴다. 그래서 걷어낼
축은 가로가 아니라 높이고, 층 고르개가 그 자리에 있다.

**세운 자재는 눌러서 못 뺀다.** 되돌리기로만 뺀다. 눌러서 빼는 길이 있으면 그것이 값 없는
되돌리기가 되어, 검수 등급이 되돌리기를 세는 뜻을 잃는다.

**나가면 그 판은 없던 일이다.** 이어서 하기를 두면 "나가서 살펴보고 다시 들어오기" 가 된다.

---

## 돈이 도는 길

| | |
|---|---|
| 버는 곳 | 판을 처음 맺을 때 갑 5 · 을 3 · 병 1 (등급 오르면 차액). 끝없이 풀기 한 판 2 |
| 쓰는 곳 | 되돌리기 5회 20닢 · 힌트 1개 15닢 |
| 광고 | 힌트 1 · 되돌리기 3 · 끝없이 되살리기 (한 판에 한 번) |
| 프리미엄 | 광고 없이 즉시 · 되돌리기 무한 · 엽전 1.5배 |

셈은 전부 서버가 한다. 클라이언트는 화면에 먼저 보이고 서버 값으로 맞춘다.

**순위표에는 돈이 안 닿는다.** 끝없이 풀기 기회를 돈으로 더 주면 등수를 사는 것이 되어
표가 죽는다. 프리미엄이 받는 것은 전부 제 지갑 안에서만 도는 것들이다.

---

## 손대 본 뒤 알게 된 것

같은 함정에 두 번 빠지지 않으려고 적어 둔다.

- `setPointerCapture` 는 `click` 을 잡아채 간다. 부지에서 누른 것을 알아내려면
  `pointerdown` 과 `pointerup` 을 짝지어야 한다.
- 컬렉션 항목을 가리키는 이름은 `id` 가 아니라 **`__id`** 다. 잘못 쓰면 아무 말 없이
  아무것도 안 지워진다.
- 광고 보상을 클라이언트에서만 더하면 다음 갱신에 서버 값으로 덮여 사라진다.
  늘리는 일은 반드시 서버를 거친다.
- `ads-verifier` 는 광고 직후에 `pending` 을 준다. `server.js` 는 `setTimeout` 이 없어
  다시 못 묻는다. 그래서 검증을 안 쓴다 — 대신 액수와 간격을 서버가 쥔다.
- Vite 는 `public/` 을 통째로 `dist` 로 복사한다. 원본 그림을 저 안에 두면 그대로 올라간다.
- `.env` 는 `envDir` 이 가리키는 곳에서만 읽는다. 루트가 `game/` 이면 저장소 루트의
  `.env` 를 지나친다.
- `requestAnimationFrame` 은 창이 안 그려지는 동안 아예 안 온다. 돌리기 같은 것은
  타이머로 돌린다.
- CSS 변수에 넣는 그림 주소는 절대 주소로 펴 두어야 한다. 상대 경로는 문서가 아니라
  스타일시트를 기준으로 풀린다.
